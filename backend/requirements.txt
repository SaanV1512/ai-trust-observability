# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=1.10.0,<2.0.0  # Use v1 for spacy compatibility

# AI/ML libraries
openai
google-generativeai
sentence-transformers
spacy>=3.7.0  # Newer version compatible with pydantic v2
numpy

# Search and data
ddgs  # DuckDuckGo search library (from ddgs import DDGS)
wikipedia-api
langdetect
pandas

# Environment
python-dotenv

# CORS support is built into FastAPI
# Note: After installing, download spacy model with:
# python -m spacy download en_core_web_sm
